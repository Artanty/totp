# Rules

- On session start, read `DECISIONS.md` and keep it in mind.
- Before any code action, write a plan to `DECISIONS.md`.
- After any code action, write progress to `DECISIONS.md`.
- Write other important things to `DECISIONS.md` anytime.
- Never `git push` (or otherwise publish to remote) unless the user explicitly asks.